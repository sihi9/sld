description: >
  DET unet default config

defaults:
  model: unet # base, unet or lanesnn
  data: det # det, carla or demo

train:
  epochs: 400
  lr: 0.0015
  amp: false
  device: auto
  save_intermediate: false

log:
  vis_interval: 20
