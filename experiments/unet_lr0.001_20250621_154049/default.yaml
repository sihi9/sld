description: >
  Recurrent UNet training configuration

defaults:
  model: unet # base or unet
  data: det   # det or demo

train:
  epochs: 50
  lr: 0.001
  amp: false
  device: auto
  save_intermediate: false

log:
  vis_interval: 10
