data:
  batch_size: 4
  heavy_noise: 0.4
  heavy_noise_prob: 0.3
  input_size:
  - 32
  - 32
  moving: true
  noise: 0.1
  num_samples: 1000
  time_steps: 10
description: 'sequence training without recurrent layer using only last timestep membrane
  potential as outout

  '
log:
  vis_interval: 10
model:
  encoder_channels:
  - 4
  - 8
  hidden_dim: null
  in_channels: 1
  init_tau: 2.0
  name: demo_b4
  out_channels: 1
  output_timesteps: 1
  recurrent: false
  use_plif_decoder: false
  use_plif_encoder: false
  use_plif_recurrent: false
train:
  amp: false
  device: cpu
  epochs: 50
  lr: 0.001
  save_intermediate: false
