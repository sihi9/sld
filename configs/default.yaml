description: >
  Carla Deep medium fully recurrent with label smoothing

defaults:
  model: unet # base, unet or lanesnn
  data: carla # det, carla or demo

train:
  epochs: 100
  lr: 0.0015
  amp: false
  device: auto
  save_intermediate: false

log:
  vis_interval: 20
