description: >
  Comparison U-Net with manual downscaling 

defaults:
  model: unet # base or unet
  data: det # det, carla or demo

train:
  epochs: 100
  lr: 0.001
  amp: false
  device: auto
  save_intermediate: false

log:
  vis_interval: 10
